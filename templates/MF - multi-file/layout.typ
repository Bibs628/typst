#let titlepage = {
  = Mein Mehrdateien-Dokument

  _Beispielhafte Struktur mit mehreren Dateien_

  #text("Autor*in", size: 12pt)
}

#let chapter = heading.with(level: 1)
#let paragraph = text.with(size: 11pt)
